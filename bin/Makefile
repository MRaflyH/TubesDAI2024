# Remove C compilation config
PYTHON = python3
SRC_DIR = ../src

all:
    $(PYTHON) $(SRC_DIR)/main.py

clean:
    find . -type f -name "*.pyc" -delete
    find . -type d -name "__pycache__" -delete
    clear